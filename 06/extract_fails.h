#ifndef GUARD_extract_fails_h
#define GUARD_extract_fails_h

#include <vector>
#include "../04/Student_info.h"
std::vector<Student_info> extract_fails_DualPassing(std::vector<Student_info>&);
std::vector<Student_info> extract_fails_MonoPassing(std::vector<Student_info>&);
#endif  // GUARD_extract_fails_h